<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Couleur de fond -->
    <stroke
            android:width="@dimen/size_one"
            android:color="@color/edit_text_outline"/> <!-- Couleur et épaisseur du contour -->
    <corners android:radius="40dp"/> <!-- Coins arrondis -->
    <padding
            android:left="@dimen/drawable_size_edit_text"
            android:top="@dimen/drawable_size_edit_text"
            android:right="@dimen/drawable_size_edit_text"
            android:bottom="@dimen/drawable_size_edit_text"/> <!-- Espacement interne -->
</shape>